let string = "A7";

function getColumn(str) {
    index = 0;
    return str.charAt(index);
}
console.log(getColumn(string)); 

function getRow(str) {
    index = 1;
    return str.charAt(index);
}
console.log(getRow(string));
